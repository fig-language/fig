# Hello, World!

The traditional first program.

```fig
fn main() {
    println("Hello, World!");
}
```

This program defines a function called `main`, which is the entry point of every Fig program. It calls the `println` function to print the string "Hello, World!" to the console.
