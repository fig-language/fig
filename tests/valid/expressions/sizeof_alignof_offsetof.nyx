// sizeof, alignof, offsetof expressions
struct Foo
    a: u8
    b: u32
    c: u64

func size_exprs() -> ok
    let sz = sizeof(Foo)
    let al = alignof(Foo)
    let off = offsetof(Foo, b)
    let sz2 = sizeof(u64)
    let al2 = alignof(*mut u8)
    pass
