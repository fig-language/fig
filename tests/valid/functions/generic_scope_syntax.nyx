// Functions with the :: generic syntax (type-associated generics)
func add::[T](a: T, b: T) -> T
    where
        T: Add[T]
    pass

func Seq[T]::new() -> Seq[T]
    pass

func Seq[T]::len(*self) -> usize
    pass
