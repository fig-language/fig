// Interface with effect marker on methods
interface Allocator
    func! allocate(*mut self, size: usize) -> *mut u8
    func! deallocate(*mut self, ptr: *mut u8) -> ok
    func! reallocate(*mut self, ptr: *mut u8, size: usize) -> *mut u8
