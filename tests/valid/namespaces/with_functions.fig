// Namespace containing function declarations
namespace math
    export func add(a: i32, b: i32) -> i32
    export func sub(a: i32, b: i32) -> i32
    export func mul(a: i32, b: i32) -> i32
    export func div(a: i32, b: i32) -> i32
